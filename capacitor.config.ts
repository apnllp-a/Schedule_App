import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schedule.schApp',
  appName: 'schedule app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
